{{ if eq .chezmoi.os "linux" -}}
#!/bin/bash
# Install mise on Linux if not present (user-space, no sudo).

if command -v mise &>/dev/null || [ -f "$HOME/.local/bin/mise" ]; then
  exit 0
fi

echo "[mise] Installing..."
curl -fsSL https://mise.jdx.dev/install.sh | bash
{{ end -}}
