# Antidote plugin configuration
# Plugins will be lazy-loaded with zsh-defer for faster startup

# Fuzzy tab completion powered by fzf
# (romkatv/zsh-defer pulled in transitively by kind:defer plugins below)
Aloxaf/fzf-tab kind:defer

# Fish-like autosuggestions from history as you type
zsh-users/zsh-autosuggestions kind:defer

# Fish-like syntax highlighting (must be last)
zsh-users/zsh-syntax-highlighting kind:defer
