{{ if eq .chezmoi.os "darwin" -}}
#!/bin/bash
# macOS system defaults. Re-runs when this file changes.

# Faster text cursor blink (default is ~500ms on/off)
defaults write -g NSTextInsertionPointBlinkPeriodOn -float 150
defaults write -g NSTextInsertionPointBlinkPeriodOff -float 100
{{ end -}}
