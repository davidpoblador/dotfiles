# Skills are managed entirely by `bunx skills`, outside chezmoi. Guard against
# accidentally pulling bunx state into the source via `chezmoi re-add ~/.agents`.
.agents/skills
.agents/.skill-lock.json

# Repo files, not dotfiles
CLAUDE.md
README.md
TODO.md
prod/
renovate.json
mise.toml
mise.*.toml

# Zed runtime state, not config (embeddings/prompts DBs, scratch tmp files)
.config/zed/conversations
.config/zed/embeddings
.config/zed/prompts
.config/zed/themes
.config/zed/.tmp*

{{- if ne .chezmoi.os "darwin" }}
# Dev-only files excluded on non-mac (prod) hosts
.codex/
.gemini/
.config/broot/
.config/ghostty/
.config/opencode/
.config/sc-im/
.config/silicon/
.config/zed/
.docker/
.ssh/
private_Library/
{{- end }}
